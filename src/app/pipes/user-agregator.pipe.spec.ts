import { UserAgregatorPipe } from './user-agregator.pipe';

describe('UserAgregatorPipe', () => {
  it('create an instance', () => {
    const pipe = new UserAgregatorPipe();
    expect(pipe).toBeTruthy();
  });
});
